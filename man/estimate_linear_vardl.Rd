% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators.R
\name{estimate_linear_vardl}
\alias{estimate_linear_vardl}
\title{Estimate a linear vector autoregressive distributed lag model}
\usage{
estimate_linear_vardl(res)
}
\arguments{
\item{res}{A list-like object produced by the model-selection workflow. It is
expected to contain at least \code{P1}, \code{P2}, \code{lag}, \code{model}, and \code{names}.}
}
\value{
A data frame of coefficient estimates and derived summaries with the
columns \code{Model}, \code{name}, \code{Estimate}, \code{SE}, \code{Tvalue}, and \code{Pval}.
}
\description{
Estimates a linear vector autoregressive distributed lag model (VARDL) from
a pre-screened model-selection result object. Because the model is not
estimated directly as a system in the original workflow, the function first
fits a linear VAR and then uses its generated regressors to estimate the
associated VARDL system with \code{systemfit}.
}
\details{
For \code{"DVARDL"}, the input series are first differenced. The function then:
\enumerate{
\item fits a linear VAR using \code{tsDyn::lineVar()},
\item extracts generated regressors and reparameterizes them into a VARDL
system estimated with \code{systemfit::systemfit()},
\item computes error-correction and long-run summaries using
\code{compute_nlcom_delta()},
\item computes symmetry and adjustment-length summaries,
\item appends log-likelihood and formats the output table.
}
}
