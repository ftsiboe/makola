% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper.R
\name{select_lag_order}
\alias{select_lag_order}
\title{Select lag order for a pair of time series}
\usage{
select_lag_order(pair, data, lag.max = 4)
}
\arguments{
\item{pair}{Character or integer vector of length 2 identifying the two
columns in \code{data} to use.}

\item{data}{A data frame or object coercible to a data frame containing the
time-series variables.}

\item{lag.max}{Positive integer giving the maximum lag order considered.
Default is \code{4}.}
}
\value{
An integer scalar giving the selected lag order. The returned value
is at least \code{2}.
}
\description{
Selects the lag order for a pair of variables using
\code{tsDyn::lags.select()}. The function subsets the requested columns,
removes incomplete observations, and returns the AIC-based lag choice.
A minimum lag order of 2 is enforced.
}
\details{
The function applies \code{tsDyn::lags.select()} to the selected pair of series
with a constant term included in the candidate models, and extracts the
AIC-minimizing lag order from the returned object. After selection, the lag
order is constrained to be no smaller than 2.
}
