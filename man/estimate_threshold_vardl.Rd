% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators.R
\name{estimate_threshold_vardl}
\alias{estimate_threshold_vardl}
\title{Estimate a threshold vector autoregressive distributed lag model}
\usage{
estimate_threshold_vardl(res)
}
\arguments{
\item{res}{A list-like object produced by the model-selection workflow. It is
expected to contain at least \code{P1}, \code{P2}, \code{lag}, \code{model}, and \code{names}.}
}
\value{
A data frame of coefficient estimates and derived summaries with the
columns \code{Model}, \code{name}, \code{Estimate}, \code{SE}, \code{Tvalue}, and \code{Pval}.
}
\description{
Estimates a threshold vector autoregressive distributed lag model (TVARDL)
from a pre-screened model-selection result object. Because the model is not
estimated directly as a system in the original workflow, the function first
fits a threshold VAR and then uses its generated regressors to estimate the
associated TVARDL system with \code{systemfit}.
}
\details{
Depending on \code{res$model}, the function fits either a two-regime or
three-regime threshold model, optionally using differenced data for
\code{"DTVARDL2"} and \code{"DTVARDL3"}. It then:
\enumerate{
\item fits a threshold VAR using \code{tsDyn::TVAR()},
\item extracts the generated regressors and reparameterizes them into a
TVARDL system estimated with \code{systemfit::systemfit()},
\item computes regime-specific error-correction and long-run summaries,
\item computes adjustment-length summaries for percentages from 1 to 99,
\item appends regime shares, thresholds, transaction-cost proxies, and
log-likelihood.
}
}
