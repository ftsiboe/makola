% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cointegration.R
\name{hansen_seo_threshold_test}
\alias{hansen_seo_threshold_test}
\title{Hansen-Seo threshold cointegration test for a pair of series}
\usage{
hansen_seo_threshold_test(
  pair,
  data,
  lag = 2,
  intercept = TRUE,
  nboot = 100,
  trim = 0.05,
  ngridTh = 100,
  verbose = FALSE
)
}
\arguments{
\item{pair}{Character or integer vector of length 2 identifying the two
columns in \code{data} to use.}

\item{data}{A data frame or object coercible to a data frame containing the
time-series variables.}

\item{lag}{Positive integer giving the lag order used in
\code{tsDyn::TVECM.HStest()}. Default is \code{2}.}

\item{intercept}{Logical indicating whether an intercept is included in the
threshold VECM. Default is \code{TRUE}.}

\item{nboot}{Positive integer giving the number of bootstrap replications
used to compute the bootstrap p-value. Default is \code{100}.}

\item{trim}{Numeric scalar in \verb{(0, 0.5)} giving the trimming proportion used
in the threshold search. Default is \code{0.05}.}

\item{ngridTh}{Positive integer giving the number of threshold grid points
used in the search. Default is \code{100}.}

\item{verbose}{Logical indicating whether to print a short message describing
the test. Default is \code{TRUE}.}
}
\value{
A data frame with one row and three columns:
\describe{
\item{Estimate}{The Hansen-Seo Sup-LM test statistic.}
\item{Pval}{Bootstrap p-value returned by \code{tsDyn::TVECM.HStest()}.}
\item{name}{Character label equal to \code{"HStest"}.}
}
}
\description{
Applies the Hansen and Seo (2002) Sup-LM test for threshold cointegration
using \code{tsDyn::TVECM.HStest()}. The test evaluates the null hypothesis of
linear cointegration against the alternative of threshold cointegration in a
two-series system.
}
\details{
The function first subsets the requested pair of variables and removes
incomplete observations. It then applies \code{tsDyn::TVECM.HStest()} to test
linear cointegration versus threshold cointegration.

Output generated by \code{tsDyn::TVECM.HStest()} is suppressed using
\code{capture.output()}. A short console message is optionally printed when
\code{verbose = TRUE}.
}
\references{
Hansen, B. E., and Seo, B. (2002). Testing for two-regime threshold
cointegration in vector error-correction models. \emph{Journal of Econometrics},
110(2), 293-318.
}
