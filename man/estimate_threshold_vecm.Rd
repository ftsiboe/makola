% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators.R
\name{estimate_threshold_vecm}
\alias{estimate_threshold_vecm}
\title{Estimate a threshold or asymmetric vector error correction model}
\usage{
estimate_threshold_vecm(res)
}
\arguments{
\item{res}{A list-like object produced by the model-selection workflow. It is
expected to contain at least:
\describe{
\item{\code{P1}, \code{P2}}{The ordered series used in estimation.}
\item{\code{URoot}}{Unit root screening results containing a \code{model} field.}
\item{\code{lag}}{Lag order used in estimation.}
\item{\code{model}}{Selected model class, such as \code{"TVECM2"}, \code{"TVECM3"}, or
\code{"AVECM"}.}
\item{\code{names}}{Identifiers for the ordered pair of series.}
}}
}
\value{
A data frame containing estimated coefficients, nonlinear
combinations, regime summaries, and model metadata. The returned columns
include:
\describe{
\item{\code{Model}}{Selected model class.}
\item{\code{name}}{Name of the estimated coefficient or derived statistic.}
\item{\code{Estimate}}{Point estimate.}
\item{\code{SE}}{Standard error when available.}
\item{\code{Tvalue}}{t-statistic when available.}
\item{\code{Pval}}{Two-sided p-value when available.}
}
}
\description{
Estimates a threshold vector error correction model (TVECM) or asymmetric
vector error correction model (AVECM) from a pre-screened model-selection
result object. The function fits the selected model, extracts coefficient and
variance-covariance information, computes nonlinear error-correction and
adjustment summaries, and returns a formatted result table.
}
\details{
The deterministic specification is chosen from \code{res$URoot$model}, with
\code{"trend"} recoded to \code{"both"} for \code{tsDyn::TVECM()}. The function then:
\enumerate{
\item fits a two- or three-regime TVECM, or an AVECM with threshold fixed
at zero,
\item extracts coefficients and their variance-covariance matrix using
\code{extract_tsdyn_vcov()},
\item computes regime-specific error-correction summaries and symmetry
measures using \code{compute_nlcom_delta()},
\item computes regime-specific adjustment-length summaries across
percentages from 1 to 99,
\item appends regime shares, thresholds, transaction-cost proxies, and
log-likelihood.
}
}
