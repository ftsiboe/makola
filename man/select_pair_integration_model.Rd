% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_pair_integration_model.R
\name{select_pair_integration_model}
\alias{select_pair_integration_model}
\title{Select a bivariate linear or threshold time-series model}
\usage{
select_pair_integration_model(
  pair,
  data,
  nboot = 100,
  lag.max = 2,
  lag.opt = 2,
  trim = 0.1
)
}
\arguments{
\item{pair}{Character or integer vector of length 2 identifying the two
columns in \code{data} to use.}

\item{data}{A data frame or object coercible to a data frame containing the
time-series variables.}

\item{nboot}{Positive integer giving the number of bootstrap replications
used in the Hansen-Seo and Hansen threshold tests. Default is \code{100}.}

\item{lag.max}{Positive integer giving the maximum lag order considered in
\code{select_lag_order()}. Default is \code{2}.}

\item{lag.opt}{Positive integer giving a user-imposed lower bound on the lag
order used in the final testing sequence. Default is \code{2}.}

\item{trim}{Numeric scalar in \verb{(0, 0.5)} giving the trimming proportion used
in \code{hansen_seo_threshold_test()}. Default is \code{0.10}.}
}
\value{
A list containing:
\describe{
\item{names}{Character vector giving the reordered series names returned by
\code{order_pair_by_granger()}.}
\item{P1}{First reordered series.}
\item{P2}{Second reordered series.}
\item{granger}{Data frame of Granger-causality test results.}
\item{URoot}{Data frame of unit root screening results from
\code{identify_i1_specification()}.}
\item{TRACE}{Data frame of Johansen trace test results from
\code{trace_pair_test()}.}
\item{HStesT}{Data frame of Hansen-Seo threshold cointegration test
results from \code{hansen_seo_threshold_test()}.}
\item{HtesT}{Data frame of Hansen linearity test results from
\code{hansen_linearity_test()}.}
\item{model}{Selected model class.}
\item{lag}{Selected lag order used in the final testing sequence.}
\item{cor}{Sample correlation matrix of the selected pair.}
\item{cov}{Sample covariance matrix of the selected pair.}
}
}
\description{
Applies a sequence of lag selection, Granger-ordering, unit root,
cointegration, and threshold diagnostic tests to a pair of time series, then
assigns a model class based on the combined test outcomes.
}
\details{
The function proceeds as follows:
\enumerate{
\item selects a lag order using \code{select_lag_order()},
\item reorders the pair using \code{order_pair_by_granger()},
\item screens the pair for I(1)-consistent behavior using
\code{identify_i1_specification()},
\item updates the lag order using the unit root output and user-supplied
lower bound,
\item applies a Johansen trace test using \code{trace_pair_test()},
\item applies the Hansen-Seo threshold cointegration test using
\code{hansen_seo_threshold_test()},
\item applies Hansen's linearity test using \code{hansen_linearity_test()},
\item selects a model class based on the resulting decision rules.
}

The candidate model classes are:
\describe{
\item{\code{"TVECM3"}}{three-regime threshold VECM}
\item{\code{"TVECM2"}}{two-regime threshold VECM}
\item{\code{"AVECM"}}{asymmetric VECM}
\item{\code{"VECM"}}{linear VECM}
\item{\code{"DTVARDL3"}}{threshold differenced VARDL with three regimes}
\item{\code{"DTVARDL2"}}{threshold differenced VARDL with two regimes}
\item{\code{"DVARDL"}}{linear differenced VARDL}
\item{\code{"TVARDL3"}}{threshold VARDL with three regimes}
\item{\code{"TVARDL2"}}{threshold VARDL with two regimes}
\item{\code{"VARDL"}}{linear VARDL}
}
}
