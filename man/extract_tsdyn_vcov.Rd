% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nonlinear_combinations_of_estimators.R
\name{extract_tsdyn_vcov}
\alias{extract_tsdyn_vcov}
\title{Extract coefficients and variance-covariance matrix from a tsDyn estimator}
\usage{
extract_tsdyn_vcov(est)
}
\arguments{
\item{est}{A fitted \code{tsDyn} model object containing residuals, model matrix,
coefficient matrices, and summary information.}
}
\value{
A list with the following components:
\describe{
\item{coef}{Named numeric vector of extracted coefficients.}
\item{df}{Residual degrees of freedom used for inference.}
\item{vcov}{Variance-covariance matrix of the extracted coefficients.}
\item{NAMES}{Data frame linking row indices, coefficient names, and
coefficient values.}
}
}
\description{
Builds a coefficient vector and variance-covariance matrix for a fitted
\code{tsDyn} model object, along with associated coefficient names and residual
degrees of freedom.
}
\details{
The function computes the residual covariance matrix from the model
residuals, applies a degrees-of-freedom adjustment, constructs the regressor
matrix used in estimation, and then forms the variance-covariance matrix
using a Kronecker product.

Coefficient names are taken from \code{summary(est)$model}, cleaned, and expanded
into equation-specific names with prefixes \code{"eq1_"} and \code{"eq2_"}.

This function assumes a two-equation \code{tsDyn} model structure with coefficient
blocks stored in \code{est$coefficients$Bdown}, \code{est$coefficients$Bmiddle}, and
\code{est$coefficients$Bup}.
}
