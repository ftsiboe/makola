% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cointegration.R
\name{trace_pair_test}
\alias{trace_pair_test}
\title{Test cointegration for a pair of time series}
\usage{
trace_pair_test(pair, data, lag, model)
}
\arguments{
\item{pair}{Character or integer vector of length 2 identifying the two
columns in \code{data} to use.}

\item{data}{A data frame or object coercible to a data frame containing the
time-series variables.}

\item{lag}{Positive integer giving the lag order passed to \code{K} in
\code{urca::ca.jo()}.}

\item{model}{Character scalar giving the deterministic specification used in
the Johansen test. This is passed to \code{ecdet}, except that \code{"both"} is
recoded to \code{"trend"}.}
}
\value{
A data frame with two rows:
\describe{
\item{Estimate}{The Johansen trace statistic for the null of no
cointegration (\code{"Trace0"}), and the sample correlation (\code{"Cor"}).}
\item{Pval}{Approximate significance level for the trace statistic
(\code{0.01}, \code{0.05}, \code{0.10}, or \code{1}), and \code{NA} for the correlation row.}
\item{name}{Label identifying the returned statistic: \code{"Trace0"} or
\code{"Cor"}.}
}
}
\description{
Applies the Johansen trace test to a pair of time series using
\code{urca::ca.jo()}, and returns the trace statistic for the null of no
cointegration together with an approximate significance level and the sample
correlation between the two series.
}
\details{
The function first subsets the requested pair of variables and removes
incomplete observations. It then applies \code{urca::ca.jo()} with
\code{type = "trace"} and evaluates the second reported trace statistic, which
corresponds to the null hypothesis of no cointegration.

The reported \code{Pval} is not an exact p-value. Instead, it is a coarse
significance bucket based on the Johansen critical values:
\describe{
\item{\code{0.01}}{reject at the 1\% level}
\item{\code{0.05}}{reject at the 5\% level but not 1\%}
\item{\code{0.10}}{reject at the 10\% level but not 5\%}
\item{\code{1}}{do not reject at the 10\% level}
}
}
