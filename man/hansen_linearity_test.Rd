% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cointegration.R
\name{hansen_linearity_test}
\alias{hansen_linearity_test}
\title{Hansen linearity test against threshold autoregression for a pair of series}
\usage{
hansen_linearity_test(
  pair,
  data,
  lag = 2,
  nboot = 100,
  thDelay = 1,
  mTh = 1,
  trend = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{pair}{Character or integer vector of length 2 identifying the two
columns in \code{data} to use.}

\item{data}{A data frame or object coercible to a data frame containing the
time-series variables.}

\item{lag}{Positive integer giving the lag order used in
\code{tsDyn::TVAR.LRtest()}. Default is \code{2}.}

\item{nboot}{Positive integer giving the number of bootstrap replications
used to compute bootstrap p-values. Default is \code{100}.}

\item{thDelay}{Positive integer giving the threshold delay parameter.
Default is \code{1}.}

\item{mTh}{Positive integer giving the number of threshold variables.
Default is \code{1}.}

\item{trend}{Logical indicating whether to include a trend term in the test.
Default is \code{TRUE}.}

\item{verbose}{Logical indicating whether to print a short message describing
the test. Default is \code{TRUE}.}
}
\value{
A data frame with three columns:
\describe{
\item{name}{Test label, either \code{"H1vs2"} or \code{"H1vs3"}.}
\item{Estimate}{Likelihood ratio test statistic for the corresponding test.}
\item{Pval}{Bootstrap p-value for the corresponding test.}
}
}
\description{
Applies Hansen's (1999) likelihood ratio test of linearity versus threshold
autoregressive alternatives using \code{tsDyn::TVAR.LRtest()}. The function
evaluates both the one-versus-two regime and one-versus-three regime
threshold alternatives for a pair of time series.
}
\details{
The function first subsets the requested pair of variables and removes
incomplete observations. It then applies \code{tsDyn::TVAR.LRtest()} with
\code{test = "1vs"} to evaluate:
\describe{
\item{\code{"H1vs2"}}{linearity versus a two-regime threshold alternative}
\item{\code{"H1vs3"}}{linearity versus a three-regime threshold alternative}
}

Output generated by \code{tsDyn::TVAR.LRtest()} is suppressed using
\code{capture.output()}. A short console message is optionally printed when
\code{verbose = TRUE}.
}
\examples{
\dontrun{
toy_data <- data.frame(
  market_1 = cumsum(rnorm(200)),
  market_2 = cumsum(rnorm(200))
)

hansen_linearity_test(
  pair = c("market_1", "market_2"),
  data = toy_data,
  lag = 2,
  nboot = 100
)
}

}
\references{
Hansen, B. E. (1999). Testing for linearity. \emph{Journal of Economic Surveys},
13(5), 551-576.
}
