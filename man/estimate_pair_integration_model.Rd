% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_pair_integration_model.R
\name{estimate_pair_integration_model}
\alias{estimate_pair_integration_model}
\title{Estimate the selected integration-based model for a pair of time series}
\usage{
estimate_pair_integration_model(
  pair,
  data,
  nboot = 100,
  lag.max = 2,
  lag.opt = 2,
  trim = 0.1,
  marketPairs = NULL,
  mpair = NULL
)
}
\arguments{
\item{pair}{Character or integer vector of length 2 identifying the two
columns in \code{data} to use.}

\item{data}{A data frame or object coercible to a data frame containing the
time-series variables.}

\item{nboot}{Positive integer giving the number of bootstrap replications
used in threshold-based model selection tests. Default is \code{100}.}

\item{lag.max}{Positive integer giving the maximum lag order considered in
\code{select_pair_integration_model()}. Default is \code{2}.}

\item{lag.opt}{Positive integer giving the user-imposed lower bound on the
lag order used in the final testing sequence. Default is \code{2}.}

\item{trim}{Numeric scalar in \verb{(0, 0.5)} giving the trimming proportion used
in threshold cointegration testing. Default is \code{0.10}.}

\item{marketPairs}{Optional data frame used to relabel \code{X1} and \code{X2} in the
final output. If supplied, it must contain columns \code{"P1"} and \code{"P2"}.}

\item{mpair}{Optional row index or row name identifying the relevant row of
\code{marketPairs}.}
}
\value{
A list returned by \code{select_pair_integration_model()} with added
components:
\describe{
\item{est}{A data frame of estimation results from the successfully fitted
model, when estimation succeeds.}
\item{estimation_error}{Character vector of captured estimation error
messages, if any estimation attempts fail.}
}

The returned list may also contain an updated \code{model} value if the function
falls back to a simpler specification during estimation.
}
\description{
Selects an integration-based model for a pair of time series using
\code{select_pair_integration_model()}, then attempts to estimate the selected
model. If estimation fails, the function progressively falls back to simpler
nested model classes until estimation succeeds.
}
\details{
The function first applies \code{select_pair_integration_model()} to determine the
preferred model class. It then attempts estimation using the corresponding
estimator:
\describe{
\item{\code{"TVECM3"}, \code{"TVECM2"}, \code{"AVECM"}}{\code{estimate_threshold_vecm()}}
\item{\code{"VECM"}}{\code{estimate_linear_vecm()}}
\item{\code{"DTVARDL2"}, \code{"DTVARDL3"}, \code{"TVARDL2"}, \code{"TVARDL3"}}{\code{estimate_threshold_vardl()}}
\item{\code{"VARDL"}, \code{"DVARDL"}}{\code{estimate_linear_vardl()}}
}

If estimation fails, the function attempts fallback models in the following
order:
\enumerate{
\item \code{TVECM3 -> TVECM2 -> AVECM -> VECM}
\item \code{TVARDL3 -> TVARDL2 -> VARDL}
\item \code{DTVARDL3 -> DTVARDL2 -> DVARDL}
}

Error messages from failed estimation attempts are stored in
\code{res$estimation_error} rather than being silently discarded.

If \code{marketPairs} and \code{mpair} are supplied, the function relabels \code{X1} and
\code{X2} in the estimation output using the corresponding market names. This
relabeling is applied only if \code{res$est} exists and contains \code{X1} and \code{X2}.
}
